spring:
  application:
    name: codex-admin

  cloud:
    config:
      uri: http://config-server:8081

  eureka:
    client:
      registerWithEureka: true
      serviceUrl.defaultZone: http://eureka-server:8080/eureka/

logging.level.org.springframework: INFO

